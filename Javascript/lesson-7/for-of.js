let colors = [`red`, `green`, `blue`];
for (let color of colors) {
  console.log(`color: ${color}`);
}
console.log("------------------------------");
let user = ` kiet dz la 1 dev dang hoc js`;
for (let char of user) {
  console.log(`char:${char}`);
}
